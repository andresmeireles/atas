import 'package:flutter/material.dart';

import '../../minute.dart';

class CreatedMinutesController extends StatelessWidget {
  const CreatedMinutesController({super.key});

  @override
  Widget build(BuildContext context) {
    return const CreatedMinutes();
  }
}
